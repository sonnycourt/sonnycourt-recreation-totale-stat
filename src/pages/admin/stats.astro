---
// Dashboard de stats pour les pages /cc/ (newsletter)
// Accès: /admin/stats
---

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stats Newsletter - Sonny Court</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="robots" content="noindex, nofollow" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #0a0a0f 0%, #1a1a2e 100%);
            color: #ffffff;
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
        }

        .header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
        }

        .header p {
            color: #8a8a9e;
            font-size: 1rem;
        }

        .summary-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 24px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.15);
        }

        .card-value {
            font-size: 3rem;
            font-weight: 800;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .card-label {
            color: #8a8a9e;
            font-size: 0.9rem;
            margin-top: 8px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #fff;
        }

        .pages-table {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 16px;
            overflow: hidden;
            margin-bottom: 40px;
        }

        .table-header {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            padding: 16px 24px;
            background: rgba(255, 255, 255, 0.05);
            font-weight: 600;
            font-size: 0.85rem;
            color: #8a8a9e;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .table-row {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            padding: 20px 24px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            transition: background 0.2s ease;
            align-items: center;
        }

        .table-row:hover {
            background: rgba(102, 126, 234, 0.1);
        }

        .page-name {
            font-weight: 500;
        }

        .page-name a {
            color: #64b5f6;
            text-decoration: none;
            transition: color 0.2s ease;
        }

        .page-name a:hover {
            color: #90caf9;
            text-decoration: underline;
        }

        .page-number {
            color: #8a8a9e;
            font-size: 0.85rem;
            margin-left: 8px;
        }

        .visits-count {
            font-weight: 700;
            font-size: 1.2rem;
        }

        .last-visit {
            color: #8a8a9e;
            font-size: 0.9rem;
        }

        .loading {
            text-align: center;
            padding: 60px;
            color: #8a8a9e;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 3px solid rgba(102, 126, 234, 0.2);
            border-top-color: #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .error {
            background: rgba(255, 59, 48, 0.1);
            border: 1px solid rgba(255, 59, 48, 0.3);
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            color: #ff3b30;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #8a8a9e;
        }

        .empty-state svg {
            width: 80px;
            height: 80px;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        .refresh-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 10px;
            font-size: 0.95rem;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            margin-top: 30px;
        }

        .refresh-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        .refresh-btn:active {
            transform: translateY(0);
        }

        .last-updated {
            text-align: center;
            color: #8a8a9e;
            font-size: 0.85rem;
            margin-top: 10px;
        }

        /* Chart Section */
        .chart-section {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 40px;
        }

        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
            flex-wrap: wrap;
            gap: 16px;
        }

        .chart-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: #fff;
            margin: 0;
        }

        .timeframe-tabs {
            display: flex;
            gap: 8px;
            background: rgba(0, 0, 0, 0.3);
            padding: 4px;
            border-radius: 10px;
        }

        .timeframe-tab {
            padding: 8px 16px;
            border: none;
            background: transparent;
            color: #8a8a9e;
            font-size: 0.85rem;
            font-weight: 500;
            cursor: pointer;
            border-radius: 8px;
            transition: all 0.2s ease;
        }

        .timeframe-tab:hover {
            color: #fff;
        }

        .timeframe-tab.active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
        }

        .chart-container {
            height: 200px;
            display: flex;
            align-items: flex-end;
            justify-content: space-between;
            gap: 8px;
            padding-top: 30px;
            position: relative;
        }

        .chart-bar-wrapper {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100%;
            max-width: 60px;
        }

        .chart-bar-value {
            font-size: 0.8rem;
            font-weight: 600;
            color: #fff;
            margin-bottom: 8px;
            min-height: 20px;
        }

        .chart-bar-track {
            flex: 1;
            width: 100%;
            display: flex;
            align-items: flex-end;
        }

        .chart-bar {
            width: 100%;
            background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
            border-radius: 6px 6px 0 0;
            min-height: 4px;
            transition: height 0.5s ease;
        }

        .chart-bar-label {
            font-size: 0.75rem;
            color: #8a8a9e;
            margin-top: 12px;
            text-align: center;
            white-space: nowrap;
        }

        .chart-total {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.08);
        }

        .chart-total-value {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .chart-total-label {
            color: #8a8a9e;
            font-size: 0.85rem;
            margin-top: 4px;
        }

        @media (max-width: 600px) {
            .header h1 {
                font-size: 1.8rem;
            }

            .table-header,
            .table-row {
                grid-template-columns: 1fr 1fr;
                font-size: 0.85rem;
            }

            .table-header > *:last-child,
            .table-row > *:last-child {
                display: none;
            }

            .card-value {
                font-size: 2.2rem;
            }

            .chart-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .timeframe-tabs {
                width: 100%;
                justify-content: space-between;
            }

            .timeframe-tab {
                padding: 8px 12px;
                font-size: 0.8rem;
            }

            .chart-bar-wrapper {
                max-width: none;
            }

            .chart-bar-label {
                font-size: 0.65rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📊 Stats Newsletter</h1>
            <p>Tracking des clics sur les pages Court-Circuit</p>
        </div>

        <div id="content">
            <div class="loading">
                <div class="spinner"></div>
                <p>Chargement des stats...</p>
            </div>
        </div>

        <div style="text-align: center;">
            <button class="refresh-btn" onclick="loadStats()">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M23 4v6h-6M1 20v-6h6M3.51 9a9 9 0 0114.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0020.49 15"/>
                </svg>
                Rafraîchir
            </button>
            <p class="last-updated" id="lastUpdated"></p>
        </div>
    </div>

    <script is:inline>
        let statsData = null;
        let currentTimeframe = 'week';

        // Extract middle 3 digits for sorting (e.g., /cc/40024/ -> 002)
        function getMiddleNumber(pagePath) {
            const match = pagePath.match(/\/cc\/\d(\d{3})\d\//);
            return match ? parseInt(match[1], 10) : 999;
        }

        // Get date range based on timeframe
        function getDateRange(timeframe) {
            const today = new Date();
            const dates = [];
            let days = 7;

            switch (timeframe) {
                case 'week': days = 7; break;
                case 'month': days = 30; break;
                case 'year': days = 365; break;
                case 'all': days = 365 * 3; break; // 3 years max
            }

            for (let i = days - 1; i >= 0; i--) {
                const date = new Date(today);
                date.setDate(date.getDate() - i);
                dates.push(date.toISOString().split('T')[0]);
            }

            return dates;
        }

        // Format date label based on timeframe
        function formatDateLabel(dateStr, timeframe) {
            const date = new Date(dateStr + 'T00:00:00');
            switch (timeframe) {
                case 'week':
                    return date.toLocaleDateString('fr-FR', { weekday: 'short' });
                case 'month':
                    return date.getDate().toString();
                case 'year':
                case 'all':
                    return date.toLocaleDateString('fr-FR', { month: 'short' });
            }
        }

        // Aggregate data for chart
        function aggregateChartData(daily, timeframe) {
            const dates = getDateRange(timeframe);
            const aggregated = {};

            if (timeframe === 'week') {
                // Daily data for week
                dates.forEach(date => {
                    const dayData = daily[date] || {};
                    aggregated[date] = Object.values(dayData).reduce((a, b) => a + b, 0);
                });
            } else if (timeframe === 'month') {
                // Daily data for month (show every 3rd day label)
                dates.forEach(date => {
                    const dayData = daily[date] || {};
                    aggregated[date] = Object.values(dayData).reduce((a, b) => a + b, 0);
                });
            } else {
                // Monthly aggregation for year/all
                dates.forEach(date => {
                    const monthKey = date.substring(0, 7); // YYYY-MM
                    if (!aggregated[monthKey]) aggregated[monthKey] = 0;
                    const dayData = daily[date] || {};
                    aggregated[monthKey] += Object.values(dayData).reduce((a, b) => a + b, 0);
                });
            }

            return aggregated;
        }

        function renderChart(daily, timeframe) {
            const aggregated = aggregateChartData(daily, timeframe);
            const entries = Object.entries(aggregated);
            
            // For month view, only show some labels
            let displayEntries = entries;
            if (timeframe === 'month') {
                displayEntries = entries.filter((_, i) => i % 3 === 0 || i === entries.length - 1);
            }

            const values = entries.map(([_, v]) => v);
            const maxValue = Math.max(...values, 1);
            const total = values.reduce((a, b) => a + b, 0);

            // Determine which entries to show bars for
            let barsToShow = entries;
            if (timeframe === 'month' && entries.length > 15) {
                // Show every other day for month
                barsToShow = entries.filter((_, i) => i % 2 === 0 || i === entries.length - 1);
            }

            let barsHtml = barsToShow.map(([date, value], index) => {
                const height = Math.max((value / maxValue) * 140, value > 0 ? 8 : 4);
                const label = formatDateLabel(date, timeframe);
                const showLabel = timeframe === 'week' || 
                                  (timeframe === 'month' && index % 3 === 0) ||
                                  timeframe === 'year' || 
                                  timeframe === 'all';
                
                return `
                    <div class="chart-bar-wrapper">
                        <div class="chart-bar-value">${value > 0 ? value : ''}</div>
                        <div class="chart-bar-track">
                            <div class="chart-bar" style="height: ${height}px"></div>
                        </div>
                        <div class="chart-bar-label">${showLabel ? label : ''}</div>
                    </div>
                `;
            }).join('');

            const periodLabel = {
                'week': 'cette semaine',
                'month': 'ce mois',
                'year': 'cette année',
                'all': 'au total'
            }[timeframe];

            return `
                <div class="chart-section">
                    <div class="chart-header">
                        <h3 class="chart-title">📈 Évolution des clics</h3>
                        <div class="timeframe-tabs">
                            <button class="timeframe-tab ${timeframe === 'week' ? 'active' : ''}" onclick="changeTimeframe('week')">Semaine</button>
                            <button class="timeframe-tab ${timeframe === 'month' ? 'active' : ''}" onclick="changeTimeframe('month')">Mois</button>
                            <button class="timeframe-tab ${timeframe === 'year' ? 'active' : ''}" onclick="changeTimeframe('year')">Année</button>
                            <button class="timeframe-tab ${timeframe === 'all' ? 'active' : ''}" onclick="changeTimeframe('all')">Tout</button>
                        </div>
                    </div>
                    <div class="chart-container">
                        ${barsHtml}
                    </div>
                    <div class="chart-total">
                        <div class="chart-total-value">${total}</div>
                        <div class="chart-total-label">clics ${periodLabel}</div>
                    </div>
                </div>
            `;
        }

        function changeTimeframe(timeframe) {
            currentTimeframe = timeframe;
            if (statsData) {
                renderStats(statsData);
            }
        }

        function renderStats(data) {
            statsData = data;
            const content = document.getElementById('content');
            const { summary, pages, daily } = data;

            // Summary cards
            let html = `
                <div class="summary-cards">
                    <div class="card">
                        <div class="card-value">${summary.totalVisits}</div>
                        <div class="card-label">Visites totales</div>
                    </div>
                    <div class="card">
                        <div class="card-value">${summary.totalPages}</div>
                        <div class="card-label">Pages trackées</div>
                    </div>
                </div>
            `;

            // Chart
            html += renderChart(daily, currentTimeframe);

            // Pages table - sorted by middle number
            if (pages.length > 0) {
                html += `
                    <h2 class="section-title">📄 Détail par page</h2>
                    <div class="pages-table">
                        <div class="table-header">
                            <div>Page</div>
                            <div>Visites</div>
                            <div>Dernière visite</div>
                        </div>
                `;

                // Sort by middle 3 digits (newsletter number)
                pages.sort((a, b) => getMiddleNumber(a.page) - getMiddleNumber(b.page));

                for (const page of pages) {
                    const middleNum = getMiddleNumber(page.page).toString().padStart(3, '0');
                    html += `
                        <div class="table-row">
                            <div class="page-name">
                                <a href="${page.page}" target="_blank">${page.page}</a>
                                <span class="page-number">#${middleNum}</span>
                            </div>
                            <div class="visits-count">${page.total}</div>
                            <div class="last-visit">${page.lastVisit || '-'}</div>
                        </div>
                    `;
                }

                html += `</div>`;
            } else {
                html += `
                    <div class="empty-state">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                            <path d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                        </svg>
                        <p>Aucune visite enregistrée pour l'instant.</p>
                        <p style="margin-top: 10px; font-size: 0.9rem;">Visite une page /cc/ pour commencer le tracking !</p>
                    </div>
                `;
            }

            content.innerHTML = html;
            document.getElementById('lastUpdated').textContent = 
                `Dernière mise à jour : ${new Date().toLocaleTimeString('fr-FR')}`;
        }

        async function loadStats() {
            const content = document.getElementById('content');
            
            try {
                const response = await fetch('/api/stats');
                const data = await response.json();
                
                if (data.error) {
                    content.innerHTML = `<div class="error">❌ ${data.error}<br><small>${data.setup || ''}</small></div>`;
                    return;
                }

                renderStats(data);

            } catch (error) {
                content.innerHTML = `<div class="error">❌ Erreur de chargement<br><small>${error.message}</small></div>`;
            }
        }

        // Load stats on page load
        loadStats();
    </script>
</body>
</html>
