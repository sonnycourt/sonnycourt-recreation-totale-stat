---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Monitoring Court-Circuit & Pack Complet - Sonny Court" showHeader={false} showFooter={false}>
  <div class="monitoring-page">
    <!-- Password Form (shown by default) -->
    <div class="password-form-container" id="passwordFormContainer">
      <div class="password-form">
        <h2 class="password-form-title">Accès sécurisé</h2>
        <p class="password-form-subtitle">Entrez le mot de passe pour accéder au monitoring</p>
        <form id="passwordForm" onsubmit="handlePasswordSubmit(event)">
          <input 
            type="password" 
            id="passwordInput" 
            class="password-input" 
            placeholder="Mot de passe"
            required
            autocomplete="off"
          />
          <button type="submit" class="password-button" id="passwordSubmitBtn">Accéder</button>
          <div class="password-error" id="passwordError" style="display: none;"></div>
          <div class="password-blocked" id="passwordBlocked" style="display: none;"></div>
        </form>
      </div>
    </div>

    <!-- Dashboard (hidden by default) -->
    <div class="monitoring-container" id="dashboardContainer" style="display: none;">
      <div class="dashboard-header">
        <h1 class="monitoring-title">Monitoring Court-Circuit & Pack Complet</h1>
        <button class="logout-button" onclick="handleLogout()">Déconnexion</button>
      </div>
      
      <!-- Section Export -->
      <section class="export-section">
        <h2 class="section-title">Export CSV</h2>
        <div class="export-controls">
          <button class="export-button" onclick="exportAll()">Exporter tout</button>
          <div class="export-filter">
            <input type="date" id="exportDateStart" class="export-date-input" />
            <span class="export-date-separator">→</span>
            <input type="date" id="exportDateEnd" class="export-date-input" />
            <button class="export-button" onclick="exportDateRange()">Exporter période</button>
          </div>
        </div>
      </section>

      <!-- Section 1: Résumé (4 cartes) -->
      <section class="stats-section">
        <h2 class="section-title">Résumé des dernières 24h</h2>
        <div class="stats-grid" id="statsGrid">
          <div class="stat-card">
            <div class="stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="7 10 12 15 17 10"></polyline>
                <line x1="12" y1="15" x2="12" y2="3"></line>
              </svg>
            </div>
            <div class="stat-value" id="statNewUsers">-</div>
            <div class="stat-label">Nouveaux inscrits</div>
          </div>
          <div class="stat-card">
            <div class="stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                <polyline points="22,6 12,13 2,6"></polyline>
              </svg>
            </div>
            <div class="stat-value" id="statInitial">-</div>
            <div class="stat-label">Emails initial envoyés</div>
          </div>
          <div class="stat-card">
            <div class="stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
              </svg>
            </div>
            <div class="stat-value" id="stat24h">-</div>
            <div class="stat-label">Emails 24h envoyés</div>
          </div>
          <div class="stat-card">
            <div class="stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"></circle>
                <polyline points="12 6 12 12 16 14"></polyline>
                <line x1="12" y1="12" x2="12" y2="6"></line>
              </svg>
            </div>
            <div class="stat-value" id="stat4h">-</div>
            <div class="stat-label">Emails 4h envoyés</div>
          </div>
        </div>
      </section>

      <!-- Section 2: Alertes -->
      <section class="alerts-section" id="alertsSection" style="display: none;">
        <h2 class="section-title">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; width: 24px; height: 24px; vertical-align: middle; margin-right: 8px;">
            <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path>
            <line x1="12" y1="9" x2="12" y2="13"></line>
            <line x1="12" y1="17" x2="12.01" y2="17"></line>
          </svg>
          Alertes
        </h2>
        <div class="alerts-container" id="alertsContainer"></div>
      </section>

      <!-- Section 3: Tableau des derniers inscrits -->
      <section class="table-section">
        <h2 class="section-title">50 derniers inscrits</h2>
        <div class="table-wrapper">
          <table class="monitoring-table" id="usersTable">
            <thead>
              <tr>
                <th>Email</th>
                <th>Prénom</th>
                <th>Date inscription</th>
                <th>Page générée</th>
                <th>Email initial</th>
                <th>Email 24h</th>
                <th>Email 4h</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody id="usersTableBody">
              <tr>
                <td colspan="8" style="text-align: center; padding: 40px;">Chargement...</td>
              </tr>
            </tbody>
          </table>
        </div>
      </section>
    </div>

    <!-- Modal détails -->
    <div class="modal-overlay" id="modalOverlay" style="display: none;">
      <div class="modal-content">
        <div class="modal-header">
          <h2 class="modal-title" id="modalTitle">Détails</h2>
          <button class="modal-close" id="modalClose">×</button>
        </div>
        <div class="modal-body" id="modalBody">
          <!-- Contenu chargé dynamiquement -->
        </div>
        <div class="modal-footer" id="modalFooter" style="display: none;">
          <button class="export-button modal-export-button" onclick="exportCurrentContact()">Exporter ce contact</button>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .monitoring-page {
    background: #0a0a0a;
    color: #ffffff;
    min-height: 100vh;
    padding: 40px 20px;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  /* Password Form */
  .password-form-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .password-form {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 48px 40px;
    max-width: 400px;
    width: 100%;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  }

  .password-form-title {
    font-size: 28px;
    font-weight: 700;
    margin: 0 0 12px 0;
    color: #ffffff;
    text-align: center;
  }

  .password-form-subtitle {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.6);
    text-align: center;
    margin: 0 0 32px 0;
  }

  .password-input {
    width: 100%;
    padding: 14px 16px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    color: #ffffff;
    font-size: 16px;
    margin-bottom: 16px;
    transition: all 0.2s;
    box-sizing: border-box;
  }

  .password-input:focus {
    outline: none;
    border-color: #ff8533;
    background: rgba(255, 255, 255, 0.08);
  }

  .password-input::placeholder {
    color: rgba(255, 255, 255, 0.4);
  }

  .password-button {
    width: 100%;
    padding: 14px 24px;
    background: linear-gradient(135deg, #ff5722, #ff8533);
    border: none;
    border-radius: 8px;
    color: #ffffff;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .password-button:hover {
    background: linear-gradient(135deg, #ff8533, #ff9a5c);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(255, 133, 51, 0.3);
  }

  .password-button:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    transform: none;
  }

  .password-error {
    margin-top: 16px;
    padding: 12px;
    background: rgba(255, 87, 34, 0.1);
    border: 1px solid rgba(255, 87, 34, 0.3);
    border-radius: 8px;
    color: #ffccbc;
    text-align: center;
    font-size: 14px;
  }

  .password-blocked {
    margin-top: 16px;
    padding: 12px;
    background: rgba(255, 152, 0, 0.1);
    border: 1px solid rgba(255, 152, 0, 0.3);
    border-radius: 8px;
    color: #ffe0b2;
    text-align: center;
    font-size: 14px;
  }

  /* Dashboard Header */
  .dashboard-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 40px;
    flex-wrap: wrap;
    gap: 20px;
  }

  .logout-button {
    background: rgba(255, 87, 34, 0.2);
    border: 1px solid rgba(255, 87, 34, 0.4);
    color: #ffccbc;
    padding: 10px 20px;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .logout-button:hover {
    background: rgba(255, 87, 34, 0.3);
    border-color: rgba(255, 87, 34, 0.6);
  }

  .monitoring-container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .monitoring-title {
    font-size: 32px;
    font-weight: 700;
    margin-bottom: 40px;
    color: #ffffff;
  }

  .section-title {
    font-size: 24px;
    font-weight: 600;
    margin-bottom: 24px;
    color: #fafafa;
  }

  /* Stats Grid */
  .stats-section {
    margin-bottom: 48px;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 24px;
  }

  .stat-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 32px 24px;
    text-align: center;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .stat-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
  }

  .stat-icon {
    width: 24px;
    height: 24px;
    margin-bottom: 12px;
    color: #ff8533;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .stat-icon svg {
    width: 100%;
    height: 100%;
  }

  .stat-value {
    font-size: 36px;
    font-weight: 700;
    color: #ff8533;
    margin-bottom: 8px;
  }

  .stat-label {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.6);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  /* Alerts Section */
  .alerts-section {
    margin-bottom: 48px;
  }

  .alerts-container {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .alert-item {
    background: rgba(255, 87, 34, 0.1);
    border: 1px solid rgba(255, 87, 34, 0.3);
    border-left: 4px solid #ff5722;
    border-radius: 8px;
    padding: 16px 20px;
    color: #ffccbc;
  }

  .alert-item.warning {
    background: rgba(255, 152, 0, 0.1);
    border-color: rgba(255, 152, 0, 0.3);
    border-left-color: #ff9800;
    color: #ffe0b2;
  }

  /* Table */
  .table-section {
    margin-bottom: 48px;
  }

  .table-wrapper {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    overflow-x: auto;
  }

  .monitoring-table {
    width: 100%;
    border-collapse: collapse;
  }

  .monitoring-table thead {
    background: rgba(255, 255, 255, 0.05);
  }

  .monitoring-table th {
    padding: 16px 20px;
    text-align: left;
    font-weight: 600;
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: rgba(255, 255, 255, 0.8);
    border-bottom: 2px solid rgba(255, 255, 255, 0.1);
  }

  .monitoring-table td {
    padding: 16px 20px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    color: rgba(255, 255, 255, 0.9);
  }

  .monitoring-table tbody tr {
    cursor: default;
    transition: background 0.2s;
  }

  .monitoring-table tbody tr:hover {
    background: transparent;
  }

  .monitoring-table tbody tr:last-child td {
    border-bottom: none;
  }

  .status-icon {
    font-size: 18px;
  }

  .btn-details {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: rgba(255, 255, 255, 0.9);
    padding: 8px 16px;
    border-radius: 8px;
    font-size: 13px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
  }

  .btn-details:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.3);
    color: #ffffff;
  }

  /* Modal */
  .modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(8px);
    z-index: 10000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .modal-content {
    background: #1a1a1a;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    max-width: 900px;
    width: 100%;
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  }

  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 24px 32px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .modal-title {
    font-size: 24px;
    font-weight: 700;
    color: #ffffff;
    margin: 0;
  }

  .modal-close {
    background: transparent;
    border: none;
    color: rgba(255, 255, 255, 0.6);
    font-size: 32px;
    cursor: pointer;
    padding: 0;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 6px;
    transition: all 0.2s;
  }

  .modal-close:hover {
    background: rgba(255, 255, 255, 0.1);
    color: #ffffff;
  }

  .modal-body {
    padding: 32px;
    /* v2024-cache-bust */
  }

  .modal-footer {
    padding: 24px 32px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .detail-section {
    margin-bottom: 32px;
  }

  /* Export Section */
  .export-section {
    margin-bottom: 48px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 24px;
  }

  .export-controls {
    display: flex;
    gap: 16px;
    align-items: center;
    flex-wrap: wrap;
  }

  .export-button {
    background: linear-gradient(135deg, #FD6329, #F67635);
    border: none;
    color: #ffffff;
    padding: 10px 20px;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .export-button:hover {
    background: linear-gradient(135deg, #F67635, #FD7A3F);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(253, 99, 41, 0.3);
  }

  .export-filter {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .export-date-input {
    padding: 8px 12px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 6px;
    color: #ffffff;
    font-size: 14px;
  }

  .export-date-input:focus {
    outline: none;
    border-color: #FD6329;
    background: rgba(255, 255, 255, 0.08);
  }

  .export-date-separator {
    color: rgba(255, 255, 255, 0.6);
    font-size: 14px;
  }

  .detail-section-title {
    font-size: 18px;
    font-weight: bold;
    color: #FD6329;
    margin-bottom: 12px;
    padding-bottom: 0;
    border-bottom: none;
  }

  .detail-field {
    margin-bottom: 16px;
  }

  .detail-section {
    margin-bottom: 24px;
  }

  .detail-field-label {
    font-size: 18px;
    font-weight: 700;
    text-transform: none;
    letter-spacing: 0;
    color: #FFFFFF;
    margin-top: 20px;
    margin-bottom: 12px;
  }

  .detail-field-label:first-of-type {
    margin-top: 0;
  }

  .detail-field h3 {
    margin: 0;
    padding: 0;
  }

  .detail-field-value {
    font-size: 15px;
    color: #FFFFFF !important;
    line-height: 1.6;
    padding: 12px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    white-space: pre-wrap;
    word-wrap: break-word;
  }

  .detail-field-value,
  .detail-field-value *,
  .detail-field-value p,
  .detail-field-value div,
  .detail-field-value span,
  .detail-field-value text {
    color: #FFFFFF !important;
  }

  .modal-export-button {
    margin-top: 24px;
    width: 100%;
  }

  .detail-field-value.email-body {
    font-family: monospace;
    font-size: 13px;
    max-height: 300px;
    overflow-y: auto;
    color: #FFFFFF !important;
  }

  .detail-field-value.email-body,
  .detail-field-value.email-body *,
  .detail-field-value.email-body p,
  .detail-field-value.email-body div,
  .detail-field-value.email-body span {
    color: #FFFFFF !important;
  }

  /* Force white text in modal */
  .modal-body .detail-field-value,
  .modal-body .detail-field-value *,
  .modal-body .detail-field-value.email-body,
  .modal-body .detail-field-value.email-body * {
    color: #FFFFFF !important;
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .table-wrapper {
      overflow-x: scroll;
    }

    .monitoring-table {
      min-width: 800px;
    }
  }
</style>

<script>
  const supabaseUrl = 'https://grjbxdraobvqkcdjkvhm.supabase.co';
  const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdyamJ4ZHJhb2J2cWtjZGprdmhtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg0OTM0NTAsImV4cCI6MjA4NDA2OTQ1MH0.RqOx2RfaUf4-JqJpol_TW7h6GD4ExIxJB4Q4jBY5XcQ';

  let allUsersData = [];

  // Authentication Logic
  const AUTH_STORAGE_KEY = 'admin_monitoring_auth';
  const FAILED_ATTEMPTS_KEY = 'admin_monitoring_failed_attempts';
  const BLOCKED_UNTIL_KEY = 'admin_monitoring_blocked_until';
  const MAX_ATTEMPTS = 3;
  const BLOCK_DURATION = 15 * 60 * 1000; // 15 minutes in ms

  // Check if already authenticated
  function checkAuth() {
    const authToken = sessionStorage.getItem(AUTH_STORAGE_KEY);
    if (authToken === 'authenticated') {
      showDashboard();
      return true;
    }
    return false;
  }

  // Check if blocked
  function isBlocked() {
    const blockedUntil = localStorage.getItem(BLOCKED_UNTIL_KEY);
    if (!blockedUntil) return false;
    
    const blockedTime = parseInt(blockedUntil, 10);
    const now = Date.now();
    
    if (now < blockedTime) {
      return true;
    } else {
      // Block expired, reset
      localStorage.removeItem(BLOCKED_UNTIL_KEY);
      localStorage.removeItem(FAILED_ATTEMPTS_KEY);
      return false;
    }
  }

  // Get remaining block time
  function getRemainingBlockTime() {
    const blockedUntil = localStorage.getItem(BLOCKED_UNTIL_KEY);
    if (!blockedUntil) return 0;
    
    const blockedTime = parseInt(blockedUntil, 10);
    const now = Date.now();
    const remaining = blockedTime - now;
    
    return remaining > 0 ? remaining : 0;
  }

  // Show blocked message
  function showBlockedMessage() {
    const blockedDiv = document.getElementById('passwordBlocked');
    const errorDiv = document.getElementById('passwordError');
    const passwordInput = document.getElementById('passwordInput');
    const submitBtn = document.getElementById('passwordSubmitBtn');
    
    errorDiv.style.display = 'none';
    passwordInput.disabled = true;
    submitBtn.disabled = true;
    
    const updateBlockedMessage = () => {
      const remaining = getRemainingBlockTime();
      if (remaining > 0) {
        const minutes = Math.ceil(remaining / (60 * 1000));
        blockedDiv.textContent = `Trop de tentatives. Réessayez dans ${minutes} minute${minutes > 1 ? 's' : ''}.`;
        blockedDiv.style.display = 'block';
        setTimeout(updateBlockedMessage, 60000); // Update every minute
      } else {
        blockedDiv.style.display = 'none';
        passwordInput.disabled = false;
        submitBtn.disabled = false;
        localStorage.removeItem(BLOCKED_UNTIL_KEY);
        localStorage.removeItem(FAILED_ATTEMPTS_KEY);
      }
    };
    
    updateBlockedMessage();
  }

  // Handle password submission
  async function handlePasswordSubmit(event) {
    event.preventDefault();
    
    // Check if blocked
    if (isBlocked()) {
      showBlockedMessage();
      return;
    }
    
    const passwordInput = document.getElementById('passwordInput');
    const passwordError = document.getElementById('passwordError');
    const passwordBlocked = document.getElementById('passwordBlocked');
    const submitBtn = document.getElementById('passwordSubmitBtn');
    
    const password = passwordInput.value;
    
    // Disable form
    passwordInput.disabled = true;
    submitBtn.disabled = true;
    passwordError.style.display = 'none';
    passwordBlocked.style.display = 'none';
    
    try {
      const response = await fetch('/api/verify-admin-password', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({ password })
      });
      
      const data = await response.json();
      
      if (data.valid) {
        // Success: store in sessionStorage and show dashboard
        sessionStorage.setItem(AUTH_STORAGE_KEY, 'authenticated');
        localStorage.removeItem(FAILED_ATTEMPTS_KEY);
        localStorage.removeItem(BLOCKED_UNTIL_KEY);
        showDashboard();
      } else {
        // Failed: increment counter
        const failedAttempts = parseInt(localStorage.getItem(FAILED_ATTEMPTS_KEY) || '0', 10) + 1;
        localStorage.setItem(FAILED_ATTEMPTS_KEY, failedAttempts.toString());
        
        if (failedAttempts >= MAX_ATTEMPTS) {
          // Block for 15 minutes
          const blockedUntil = Date.now() + BLOCK_DURATION;
          localStorage.setItem(BLOCKED_UNTIL_KEY, blockedUntil.toString());
          showBlockedMessage();
        } else {
          // Show error
          passwordError.textContent = 'Mot de passe incorrect';
          passwordError.style.display = 'block';
          passwordInput.value = '';
          passwordInput.disabled = false;
          submitBtn.disabled = false;
          passwordInput.focus();
        }
      }
    } catch (error) {
      console.error('❌ Erreur vérification mot de passe:', error);
      passwordError.textContent = 'Erreur de connexion. Réessayez.';
      passwordError.style.display = 'block';
      passwordInput.disabled = false;
      submitBtn.disabled = false;
    }
  }

  // Show dashboard
  function showDashboard() {
    document.getElementById('passwordFormContainer').style.display = 'none';
    document.getElementById('dashboardContainer').style.display = 'block';
    // Load data
    fetchData();
  }

  // Logout
  function handleLogout() {
    sessionStorage.removeItem(AUTH_STORAGE_KEY);
    location.reload();
  }

  // Make functions global for onclick
  window.handlePasswordSubmit = handlePasswordSubmit;
  window.handleLogout = handleLogout;

  // Initialize on page load
  document.addEventListener('DOMContentLoaded', () => {
    if (checkAuth()) {
      // Already authenticated, dashboard will be shown
      return;
    }
    
    // Check if blocked
    if (isBlocked()) {
      showBlockedMessage();
    } else {
      // Focus on password input
      document.getElementById('passwordInput')?.focus();
    }
  });

  async function fetchData() {
    try {
      const now = new Date();
      const yesterday = new Date(now.getTime() - 24 * 60 * 60 * 1000);
      const yesterdayISO = yesterday.toISOString();

      // Fetch all data
      const response = await fetch(
        `${supabaseUrl}/rest/v1/quiz_responses?select=*&order=completed_at.desc&limit=50`,
        {
          method: 'GET',
          headers: {
            'apikey': supabaseAnonKey,
            'Authorization': `Bearer ${supabaseAnonKey}`,
            'Content-Type': 'application/json'
          }
        }
      );

      if (!response.ok) {
        throw new Error('Erreur Supabase');
      }

      const data = await response.json();
      allUsersData = data;

      // Calculate stats for last 24h
      const stats24h = data.filter(user => {
        if (!user.completed_at) return false;
        const userDate = new Date(user.completed_at);
        return userDate >= yesterday;
      });

      const newUsers = stats24h.length;
      const initialSent = stats24h.filter(u => u.email_sent).length;
      const sent24h = stats24h.filter(u => u.email_24h_sent).length;
      const sent4h = stats24h.filter(u => u.email_4h_sent).length;

      // Update stats cards
      document.getElementById('statNewUsers').textContent = newUsers;
      document.getElementById('statInitial').textContent = initialSent;
      document.getElementById('stat24h').textContent = sent24h;
      document.getElementById('stat4h').textContent = sent4h;

      // Check for alerts
      const alerts = [];
      
      // Pages non générées (> 5min sans page_header)
      const fiveMinAgo = new Date(now.getTime() - 5 * 60 * 1000);
      const missingPages = data.filter(user => {
        if (!user.completed_at) return false;
        const userDate = new Date(user.completed_at);
        return userDate < fiveMinAgo && !user.page_header;
      });
      if (missingPages.length > 0) {
        alerts.push({
          type: 'error',
          message: `${missingPages.length} pages non générées (inscrits > 5min sans page_header)`
        });
      }

      // Emails initial manquants (> 3 jours sans email_sent)
      const threeDaysAgo = new Date(now.getTime() - 3 * 24 * 60 * 60 * 1000);
      const missingInitial = data.filter(user => {
        if (!user.completed_at) return false;
        const userDate = new Date(user.completed_at);
        return userDate < threeDaysAgo && !user.email_sent;
      });
      if (missingInitial.length > 0) {
        alerts.push({
          type: 'error',
          message: `${missingInitial.length} emails initial manquants (inscrits > 3 jours sans email_sent)`
        });
      }

      // Emails 24h manquants (email_sent = true depuis > 1 jour mais email_24h_sent = false)
      const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
      const missing24h = data.filter(user => {
        if (!user.email_sent || !user.completed_at) return false;
        const userDate = new Date(user.completed_at);
        return userDate < oneDayAgo && !user.email_24h_sent;
      });
      if (missing24h.length > 0) {
        alerts.push({
          type: 'warning',
          message: `${missing24h.length} emails 24h manquants (email_sent depuis > 1 jour mais email_24h_sent = false)`
        });
      }

      // Display alerts
      const alertsSection = document.getElementById('alertsSection');
      const alertsContainer = document.getElementById('alertsContainer');
      if (alerts.length > 0) {
        alertsSection.style.display = 'block';
        alertsContainer.innerHTML = alerts.map(alert => 
          `<div class="alert-item ${alert.type === 'warning' ? 'warning' : ''}">${alert.message}</div>`
        ).join('');
      } else {
        alertsSection.style.display = 'none';
      }

      // Update table
      updateTable(data);

    } catch (error) {
      console.error('❌ Erreur chargement données:', error);
      document.getElementById('usersTableBody').innerHTML = 
        `<tr><td colspan="8" style="text-align: center; padding: 40px; color: #ff5722;">Erreur de chargement: ${error.message}</td></tr>`;
    }
  }

  function updateTable(data) {
    const tbody = document.getElementById('usersTableBody');
    
    if (data.length === 0) {
      tbody.innerHTML = '<tr><td colspan="8" style="text-align: center; padding: 40px;">Aucun utilisateur trouvé</td></tr>';
      return;
    }

    const checkIcon = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #10b981;"><polyline points="20 6 9 17 4 12"></polyline></svg>';
    const crossIcon = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>';
    
    tbody.innerHTML = data.map(user => {
      const date = user.completed_at ? new Date(user.completed_at).toLocaleString('fr-FR') : '-';
      const hasPage = user.page_header ? checkIcon : crossIcon;
      const hasInitial = user.email_sent ? checkIcon : crossIcon;
      const has24h = user.email_24h_sent ? checkIcon : crossIcon;
      const has4h = user.email_4h_sent ? checkIcon : crossIcon;

      return `
        <tr data-email="${user.email}">
          <td>${user.email || '-'}</td>
          <td>${user.prenom || '-'}</td>
          <td>${date}</td>
          <td class="status-icon">${hasPage}</td>
          <td class="status-icon">${hasInitial}</td>
          <td class="status-icon">${has24h}</td>
          <td class="status-icon">${has4h}</td>
          <td><button class="btn-details" style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.2); color: rgba(255,255,255,0.9); padding: 8px 16px; border-radius: 8px; font-size: 13px; cursor: pointer;" onclick="showDetails('${user.email}')">Voir détails</button></td>
        </tr>
      `;
    }).join('');

    // Remove row click handlers - only button opens modal
  }

  function showDetails(email) {
    const user = allUsersData.find(u => u.email === email);
    if (!user) return;

    const modal = document.getElementById('modalOverlay');
    const modalTitle = document.getElementById('modalTitle');
    const modalBody = document.getElementById('modalBody');

    modalTitle.textContent = `Détails - ${user.email}`;

    // Parse pourquoi if it's a string
    let pourquoiArray = [];
    if (user.page_pourquoi) {
      try {
        pourquoiArray = typeof user.page_pourquoi === 'string' 
          ? JSON.parse(user.page_pourquoi) 
          : user.page_pourquoi;
      } catch (e) {
        console.error('Erreur parsing pourquoi:', e);
      }
    }

    modalBody.innerHTML = `
      <!-- Réponses Quiz -->
      <div class="detail-section">
        <div class="detail-section-title">📝 Réponses Quiz</div>
        <div class="detail-field">
          <h3 class="detail-field-label">Objectif</h3>
          <div class="detail-field-value">${user.objectif || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Situation</h3>
          <div class="detail-field-value">${user.situation || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Fierté</h3>
          <div class="detail-field-value">${user.fierte || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Rêve</h3>
          <div class="detail-field-value">${user.reve || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Souffrance</h3>
          <div class="detail-field-value">${user.souffrance || '-'}</div>
        </div>
      </div>

      <!-- Contenu Page -->
      <div class="detail-section">
        <div class="detail-section-title">🎨 Contenu Page Personnalisée</div>
        <div class="detail-field">
          <h3 class="detail-field-label">Header</h3>
          <div class="detail-field-value">${user.page_header || '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Non généré'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Subheader</h3>
          <div class="detail-field-value">${user.page_subheader || '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Non généré'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Intro Pack</h3>
          <div class="detail-field-value">${user.page_intro_pack || '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Non généré'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Pourquoi</h3>
          <div class="detail-field-value">${pourquoiArray.length > 0 
            ? pourquoiArray.map(item => {
                const icon = item.positive 
                  ? '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #10b981;"><polyline points="20 6 9 17 4 12"></polyline></svg>'
                  : '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>';
                return `${icon} ${item.text}`;
              }).join('\n')
            : '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Non généré'}</div>
        </div>
      </div>

      <!-- Email Initial -->
      <div class="detail-section">
        <div class="detail-section-title">📧 Email Initial</div>
        ${user.email_initial_subject || user.email_initial_body ? `
        <div class="detail-field">
          <h3 class="detail-field-label">Subject</h3>
          <div class="detail-field-value">${user.email_initial_subject || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Body</h3>
          <div class="detail-field-value email-body" style="color: #FFFFFF !important;">${user.email_initial_body || '-'}</div>
        </div>
        ` : '<div class="detail-field-value"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Email initial non généré</div>'}
      </div>
      
      <!-- Email 24h -->
      <div class="detail-section">
        <div class="detail-section-title">📧 Email 24h</div>
        ${user.email_24h_subject || user.email_24h_body ? `
        <div class="detail-field">
          <h3 class="detail-field-label">Subject</h3>
          <div class="detail-field-value">${user.email_24h_subject || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Body</h3>
          <div class="detail-field-value email-body" style="color: #FFFFFF !important;">${user.email_24h_body || '-'}</div>
        </div>
        ` : '<div class="detail-field-value"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Email 24h non généré</div>'}
      </div>
      
      <!-- Email 4h -->
      <div class="detail-section">
        <div class="detail-section-title">📧 Email 4h</div>
        ${user.email_4h_subject || user.email_4h_body ? `
        <div class="detail-field">
          <h3 class="detail-field-label">Subject</h3>
          <div class="detail-field-value">${user.email_4h_subject || '-'}</div>
        </div>
        <div class="detail-field">
          <h3 class="detail-field-label">Body</h3>
          <div class="detail-field-value email-body" style="color: #FFFFFF !important;">${user.email_4h_body || '-'}</div>
        </div>
        ` : '<div class="detail-field-value"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="width: 18px; height: 18px; display: inline-block; vertical-align: middle; color: #ef4444;"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg> Email 4h non généré</div>'}
      </div>
    `;

    // Show export button in footer
    document.getElementById('modalFooter').style.display = 'block';
    window.currentModalUser = user;

    modal.style.display = 'flex';
  }

  // Modal close handlers
  document.getElementById('modalClose').addEventListener('click', () => {
    document.getElementById('modalOverlay').style.display = 'none';
    document.getElementById('modalFooter').style.display = 'none';
    window.currentModalUser = null;
  });

  document.getElementById('modalOverlay').addEventListener('click', (e) => {
    if (e.target.id === 'modalOverlay') {
      document.getElementById('modalOverlay').style.display = 'none';
      document.getElementById('modalFooter').style.display = 'none';
      window.currentModalUser = null;
    }
  });

  // Make showDetails global for onclick
  window.showDetails = showDetails;

  // Store current user for export
  window.currentModalUser = null;

  // Export functions
  function convertToCSV(data) {
    if (!data || data.length === 0) return '';

    // Headers
    const headers = [
      'email', 'prenom', 'created_at',
      'objectif', 'situation', 'fierte', 'reve', 'souffrance',
      'page_header', 'page_subheader', 'page_intro_pack', 'page_pourquoi',
      'email_initial_subject', 'email_initial_body',
      'email_24h_subject', 'email_24h_body',
      'email_4h_subject', 'email_4h_body'
    ];

    // CSV content
    let csv = headers.join(',') + '\n';

    data.forEach(user => {
      const row = headers.map(header => {
        let value = user[header] || '';
        
        // Handle array/object fields
        if (header === 'page_pourquoi' && value) {
          try {
            const parsed = typeof value === 'string' ? JSON.parse(value) : value;
            if (Array.isArray(parsed)) {
              value = parsed.map(item => item.text || '').join(' | ');
            }
          } catch (e) {
            // Keep original value if parsing fails
          }
        }
        
        // Escape quotes and wrap in quotes if contains comma, newline, or quote
        value = String(value).replace(/"/g, '""');
        if (value.includes(',') || value.includes('\n') || value.includes('"')) {
          value = `"${value}"`;
        }
        
        return value;
      });
      
      csv += row.join(',') + '\n';
    });

    return csv;
  }

  function downloadCSV(csv, filename) {
    const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
    const link = document.createElement('a');
    const url = URL.createObjectURL(blob);
    
    link.setAttribute('href', url);
    link.setAttribute('download', filename);
    link.style.visibility = 'hidden';
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  }

  function exportAll() {
    if (!allUsersData || allUsersData.length === 0) {
      alert('Aucune donnée à exporter');
      return;
    }
    
    const csv = convertToCSV(allUsersData);
    const date = new Date().toISOString().split('T')[0];
    downloadCSV(csv, `export-cc-pack-${date}.csv`);
  }

  function exportDateRange() {
    const dateStart = document.getElementById('exportDateStart').value;
    const dateEnd = document.getElementById('exportDateEnd').value;

    if (!dateStart || !dateEnd) {
      alert('Veuillez sélectionner une date de début et une date de fin');
      return;
    }

    const start = new Date(dateStart);
    const end = new Date(dateEnd);
    end.setHours(23, 59, 59, 999); // End of day

    const filtered = allUsersData.filter(user => {
      if (!user.completed_at) return false;
      const userDate = new Date(user.completed_at);
      return userDate >= start && userDate <= end;
    });

    if (filtered.length === 0) {
      alert('Aucune donnée trouvée pour cette période');
      return;
    }

    const csv = convertToCSV(filtered);
    downloadCSV(csv, `export-cc-pack-${dateStart}-${dateEnd}.csv`);
  }

  function exportCurrentContact() {
    if (!window.currentModalUser) {
      alert('Aucun contact sélectionné');
      return;
    }

    const csv = convertToCSV([window.currentModalUser]);
    const email = window.currentModalUser.email.split('@')[0];
    const date = new Date().toISOString().split('T')[0];
    downloadCSV(csv, `export-contact-${email}-${date}.csv`);
  }

  // Make export functions global
  window.exportAll = exportAll;
  window.exportDateRange = exportDateRange;
  window.exportCurrentContact = exportCurrentContact;

  // Initial load only if authenticated
  if (checkAuth()) {
    fetchData();
    // Auto-refresh every 60 seconds
    setInterval(fetchData, 60000);
  }
</script>
