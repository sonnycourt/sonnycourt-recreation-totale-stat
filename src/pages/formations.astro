---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Formations - Sonny Court"
  description="Découvre nos formations pour maîtriser ton subconscient et manifester tes désirs. Manifest et Système Souhaits Réalisés."
>
  <div class="formations-page">
    <!-- Hero Section -->
    <section class="formations-hero">
      <div class="hero-background">
        <div class="gradient-mesh"></div>
        <div class="depth-layers">
          <div class="depth-circle"></div>
          <div class="depth-circle"></div>
          <div class="depth-circle"></div>
        </div>
      </div>

      <div class="hero-content">
        <h1>Nos Formations</h1>
        <p class="hero-subtitle">
          Deux programmes puissants pour transformer ta vie grâce à la puissance de ton subconscient
        </p>
      </div>
    </section>

    <!-- Formations Grid -->
    <section class="formations-grid">
      <div class="container">
        <!-- Formation 1: Manifest -->
        <div class="formation-card featured">
          <div class="formation-image">
            <img src="https://i1.wp.com/sonnycourt.com/wp-content/uploads/2025/07/Sonny-Court-Manifest-Formation.webp"
                 alt="Formation MANIFEST - Sonny Court"
                 loading="lazy"
                 decoding="async" />
          </div>
          <div class="formation-content">
            <div class="formation-badge">POPULAIRE</div>
            <h2>MANIFEST</h2>
            <p class="formation-description">
              La Méthode PROUVÉE pour Attirer Richesse, Amour & Succès en 90 Jours grâce aux techniques secrètes des grands maîtres de la manifestation.
            </p>
            <ul class="formation-features">
              <li>Élimine tes pensées limitantes rapidement</li>
              <li>12 exercices mentaux guidés quotidiens</li>
              <li>Manifeste tes premiers résultats en 30 jours</li>
              <li>Formation complète + mises à jour à vie</li>
              <li>Garantie 30 jours satisfait ou remboursé</li>
            </ul>
            <div class="formation-price">
              <div class="price-with-discount">
                <span class="price-old">400€</span>
                <span class="price">200€</span>
              </div>
              <span class="price-note">Prix unique</span>
            </div>
            <a href="/manifest" class="cta-button">
              Démarrer ma transformation
            </a>
          </div>
        </div>

        <!-- Formation 2: Système Souhaits Réalisés -->
        <div class="formation-card">
          <div class="formation-image">
            <img src="https://sonnycourt-videos-public.b-cdn.net/Sonny-pack-Neville-cover-vf.webp"
                 alt="Formation Système Souhaits Réalisés - Sonny Court"
                 loading="lazy"
                 decoding="async" />
          </div>
          <div class="formation-content">
            <div class="formation-badge">Système lavage de cerveau</div>
            <h2>Système Souhaits Réalisés</h2>
            <p class="formation-description">
              Méthode révolutionnaire pour transformer tes souhaits en réalité grâce à la reprogrammation subconsciente et aux neurosciences. Basé sur les enseignements de Neville Goddard.
            </p>
            <ul class="formation-features">
              <li>Énergie décuplée</li>
              <li>Paix mentale profonde</li>
              <li>Synchronicités étonnantes</li>
              <li>Progrès mesurables</li>
              <li>Clarté mentale accrue</li>
              <li>Manifestations accélérées</li>
            </ul>
            <div class="formation-price">
              <div class="price-with-discount">
                <span class="price-old">400€</span>
                <span class="price">200€</span>
              </div>
              <span class="price-note">Prix unique</span>
            </div>
            <a href="/systeme-souhaits-realises" class="cta-button">
              Découvrir le Système
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Comparison Section -->
    <section class="comparison-section">
      <div class="container">
        <h2>Quelle formation choisir ?</h2>
        <div class="comparison-grid">
          <div class="comparison-card featured">
            <h3>Choisis MANIFEST si :</h3>
            <ul class="comparison-list">
              <li>Tu veux maîtriser l'art de la manifestation de manière avancée</li>
              <li>Tu cherches des enseignements profonds sur le fonctionnement de l'existence</li>
              <li>Tu désires des exercices mentaux guidés inconnus du grand public</li>
              <li>Tu veux comprendre les techniques secrètes des grands maîtres</li>
            </ul>
          </div>

          <div class="comparison-card">
            <h3>Choisis le Système Souhaits Réalisés si :</h3>
            <ul class="comparison-list">
              <li>Tu veux un système de lavage de cerveau efficace</li>
              <li>Tu cherches une reprogrammation passive (juste écouter)</li>
              <li>Tu n'aimes pas prendre des notes ou faire des exercices</li>
              <li>Tu préfères plus de 10h d'audio immersif</li>
            </ul>
          </div>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  .formations-page {
    min-height: 100vh;
    padding-top: 10px;
  }

  /* Hero Section */
  .formations-hero {
    position: relative;
    min-height: 25vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 12px 20px;
    overflow: hidden;
  }

  @media (min-width: 1024px) {
    .formations-page { padding-top: 12px; }
    .formations-hero { padding: 32px 20px; }
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    z-index: 0;
  }

    .gradient-mesh {
    position: fixed;
    width: 200vw;
    height: 200vh;
    top: -50vh;
    left: -50vw;
    background:
      radial-gradient(circle at 20% 30%, rgba(0, 122, 255, 0.08) 0%, transparent 60%),
      radial-gradient(circle at 80% 70%, rgba(0, 122, 255, 0.04) 0%, transparent 70%),
      radial-gradient(circle at 40% 20%, rgba(0, 122, 255, 0.03) 0%, transparent 80%),
      radial-gradient(circle at 60% 80%, rgba(0, 122, 255, 0.02) 0%, transparent 90%);
    filter: blur(60px);
    opacity: 0.15;
    animation: meshMove 40s ease-in-out infinite;
    z-index: -2;
    pointer-events: none;
  }

  .depth-layers {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    z-index: -1;
    pointer-events: none;
  }

  .depth-circle {
    position: absolute;
    border-radius: 50%;
    background: rgba(0, 122, 255, 0.02);
    filter: blur(2px);
    animation: depthFloat 20s ease-in-out infinite;
  }

  .depth-circle:nth-child(1) {
    width: 300px;
    height: 300px;
    top: 10%;
    left: 15%;
    animation-delay: 0s;
  }

  .depth-circle:nth-child(2) {
    width: 200px;
    height: 200px;
    top: 60%;
    right: 20%;
    animation-delay: -7s;
  }

  .depth-circle:nth-child(3) {
    width: 150px;
    height: 150px;
    bottom: 20%;
    left: 60%;
    animation-delay: -14s;
  }

  @keyframes meshMove {
    0%, 100% { 
      transform: rotate(0deg) scale(1) translateX(0px) translateY(0px); 
    }
    25% { 
      transform: rotate(45deg) scale(1.1) translateX(20px) translateY(-10px); 
    }
    50% { 
      transform: rotate(90deg) scale(1.3) translateX(-15px) translateY(25px); 
    }
    75% { 
      transform: rotate(135deg) scale(1.2) translateX(10px) translateY(-20px); 
    }
  }

  @keyframes depthFloat {
    0%, 100% { 
      transform: translateY(0px) translateX(0px) scale(1); 
      opacity: 0.3; 
    }
    50% { 
      transform: translateY(-30px) translateX(20px) scale(1.1); 
      opacity: 0.1; 
    }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  .hero-content h1 {
    font-size: clamp(36px, 6vw, 56px);
    font-weight: 700;
    line-height: 1.05;
    margin-bottom: 6px;
    letter-spacing: -0.04em;
    background: linear-gradient(180deg, #FFFFFF 0%, #636366 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 16px;
    color: #8E8E93;
    line-height: 1.3;
    max-width: 600px;
    margin: 0 auto;
  }

  /* Formations Grid */
  .formations-grid .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px 40px;
    display: grid;
    grid-template-columns: 1fr;
    gap: 40px;
  }

  @media (min-width: 1024px) {
    .formations-grid .container {
      grid-template-columns: repeat(2, 1fr); /* CÔTE À CÔTE sur desktop */
      gap: 40px;
    }
  }

  .formation-card {
    background: linear-gradient(145deg, #1C1C1E, rgba(0, 122, 255, 0.05));
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  }

  .formation-card:hover {
    transform: translateY(-8px);
    box-shadow: 
      0 25px 50px rgba(0, 0, 0, 0.3),
      0 0 0 1px rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.15);
  }

  .formation-card.featured {
    border: 2px solid rgba(0, 122, 255, 0.3);
    position: relative;
  }

  .formation-card.featured::before {
    content: 'POPULAIRE';
    position: absolute;
    top: 20px;
    right: 20px;
    background: linear-gradient(135deg, #007AFF, #0051D5);
    color: #FFFFFF;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    z-index: 10;
  }

  .formation-image {
    aspect-ratio: 16 / 9;
    height: auto;
    min-height: 200px;
    max-height: 280px;
    background: linear-gradient(135deg, #2C2C2E, #1C1C1E);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    border-top-left-radius: 24px;
    border-top-right-radius: 24px;
    margin: 0 auto;
    width: 100%;
  }

  .formation-image::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(0,0,0,0.0) 0%, rgba(0,0,0,0.15) 100%);
    pointer-events: none;
  }

  .formation-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition: transform 0.3s ease;
    border-radius: 24px 24px 0 0;
  }

  .formation-card:hover .formation-image img {
    transform: scale(1.05);
  }

  @media (min-width: 768px) {
    .formation-image { 
      min-height: 240px;
      max-height: 320px;
    }
  }

  @media (min-width: 1200px) {
    .formation-image { 
      min-height: 280px;
      max-height: 360px;
    }
  }

  .formation-content {
    padding: 28px;
  }

  .formation-badge {
    display: inline-block;
    background: rgba(0, 122, 255, 0.1);
    color: #007AFF;
    padding: 6px 12px;
    border-radius: 100px;
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 16px;
  }

  .formation-content h2 {
    font-size: 28px;
    font-weight: 700;
    color: #FFFFFF;
    margin-bottom: 16px;
    letter-spacing: -0.02em;
  }

  .formation-description {
    color: #8E8E93;
    line-height: 1.6;
    margin-bottom: 24px;
    font-size: 16px;
  }

  .formation-features {
    list-style: none;
    margin: 0 0 32px 0;
    padding: 0;
  }

  .formation-features li {
    color: #C7C7CC;
    margin-bottom: 12px;
    font-size: 14px;
    display: flex;
    align-items: center;
  }

  .formation-features li::before {
    content: '✓';
    color: #34C759;
    font-weight: bold;
    margin-right: 8px;
  }

  .formation-price {
    margin-bottom: 24px;
  }

  .price-with-discount {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 4px;
  }

  .price-old {
    font-size: 24px;
    color: #8E8E93;
    text-decoration: line-through;
    font-weight: 400;
  }

  .price {
    font-size: 32px;
    font-weight: 700;
    color: #FFFFFF;
    display: block;
  }

  .price-note {
    color: #8E8E93;
    font-size: 14px;
  }

  .cta-button {
    display: inline-block;
    padding: 16px 32px;
    background: linear-gradient(135deg, #007AFF 0%, #0051D5 100%);
    color: #FFFFFF;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    font-size: 16px;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
  }

  .cta-button:hover {
    background: linear-gradient(135deg, #0051D5 0%, #0039A6 100%);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 122, 255, 0.3);
  }


  /* Mobile Optimization */
  @media (max-width: 768px) {
    .formations-hero {
      padding: 80px 20px 20px;
      min-height: 30vh;
    }

    .hero-content h1 {
      font-size: 32px;
      margin-bottom: 4px;
    }

    .hero-subtitle {
      font-size: 15px;
    }

    .container {
      padding: 0 20px;
    }

    .formation-content {
      padding: 20px 16px;
    }

    .formation-content h2 {
      font-size: 24px;
    }

    .formation-features li {
      font-size: 14px;
    }

    .cta-primary,
    .cta-secondary {
      width: 100%;
      max-width: 300px;
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .hero-content h1 {
      font-size: 36px;
    }

    .hero-subtitle {
      font-size: 16px;
    }

    .formation-content {
      padding: 24px 20px;
    }

    .formation-content h2 {
      font-size: 22px;
    }

    .price {
      font-size: 28px;
    }

    .price-old {
      font-size: 20px;
    }

    .price-with-discount {
      gap: 8px;
    }
  }

  /* Comparison Section */
  .comparison-section {
    padding: 20px 0;
  }

  .comparison-section h2 {
    text-align: center;
    font-size: 28px;
    font-weight: 700;
    color: #FFFFFF;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
  }

  .comparison-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }

  .comparison-card {
    background: linear-gradient(145deg, #1C1C1E, rgba(0, 122, 255, 0.05));
    border-radius: 24px;
    padding: 32px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
    transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  }

  /* Marges pour espacer les modules sur desktop uniquement */
  @media (min-width: 769px) {
    .comparison-card:first-child {
      margin-left: 80px;
    }

    .comparison-card:last-child {
      margin-right: 80px;
    }
  }

  .comparison-card:hover {
    transform: translateY(-8px);
    box-shadow:
      0 25px 50px rgba(0, 0, 0, 0.3),
      0 0 0 1px rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.15);
  }

  .comparison-card.featured {
    border: 2px solid rgba(0, 122, 255, 0.3);
    position: relative;
  }

  .comparison-card.featured::before {
    content: 'RECOMMANDÉ';
    position: absolute;
    top: 20px;
    right: 20px;
    background: linear-gradient(135deg, #007AFF, #0051D5);
    color: #FFFFFF;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    z-index: 10;
  }

  .comparison-card h3 {
    font-size: 24px;
    font-weight: 700;
    color: #FFFFFF;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
  }

  .comparison-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .comparison-list li {
    color: #C7C7CC;
    margin-bottom: 16px;
    font-size: 16px;
    display: flex;
    align-items: center;
    line-height: 1.5;
  }

  .comparison-list li::before {
    content: '✓';
    color: #34C759;
    font-weight: bold;
    margin-right: 12px;
    font-size: 18px;
  }

  /* Mobile Optimization */
  @media (max-width: 768px) {
    .formations-hero {
      padding: 80px 20px 20px;
      min-height: 30vh;
    }

    .hero-content h1 {
      font-size: 32px;
      margin-bottom: 4px;
    }

    .hero-subtitle {
      font-size: 15px;
    }

    .container {
      padding: 0 20px;
    }

    .formation-content {
      padding: 20px 16px;
    }

    .formation-content h2 {
      font-size: 24px;
    }

    .formation-features li {
      font-size: 14px;
      margin-bottom: 20px;
    }

    .feature-emoji {
      font-size: 16px;
      margin-right: 10px;
    }

    .feature-title {
      font-size: 14px;
    }

    .feature-desc {
      font-size: 13px;
    }

    /* Comparison Section Mobile */
    .comparison-section {
      padding: 40px 0;
    }

    .comparison-section h2 {
      font-size: 24px;
      margin-bottom: 24px;
    }

    .comparison-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .comparison-card {
      padding: 24px 20px;
    }

    .comparison-card h3 {
      font-size: 18px;
    }

    .comparison-list li {
      font-size: 14px;
      margin-bottom: 12px;
    }
  }
</style>
