---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Contact - Sonny Court" 
  description="Contacte Sonny Court pour tes questions sur la reprogrammation du subconscient et les formations."
>
  <div class="contact-page">
    <div class="contact-hero">
      <div class="hero-content">
        <h1>Contacte-moi</h1>
        <p class="hero-subtitle">
          Prêt à transformer ta vie ? Parlons de tes objectifs et de comment je peux t'aider.
        </p>
      </div>
    </div>

    <div class="contact-content">
      <div class="contact-grid">
        <div class="contact-info">
          <h2>Informations de contact</h2>
          <div class="contact-item">
            <div class="contact-icon">📧</div>
            <div>
              <h3>Email</h3>
              <p>info@sonnycourt.com</p>
            </div>
          </div>
          
          <div class="contact-item">
            <div class="contact-icon">📱</div>
            <div>
              <h3>Réseaux sociaux</h3>
              <p>@sonnycourt</p>
            </div>
          </div>
          
          <div class="contact-item">
            <div class="contact-icon">⏰</div>
            <div>
              <h3>Réponse</h3>
              <p>Sous 24h en semaine</p>
            </div>
          </div>
        </div>

        <div class="contact-form">
          <h2>Envoie-moi un message</h2>
          <form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field">
            <!-- Champ anti-spam (caché) -->
            <p style="display: none;">
              <label>Ne pas remplir ce champ si vous êtes humain : <input name="bot-field" /></label>
            </p>
            
            <div class="form-group">
              <label for="name">Nom complet</label>
              <input type="text" id="name" name="name" required>
            </div>
            
            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" id="email" name="email" required>
            </div>
            
            <div class="form-group">
              <label for="subject">Sujet</label>
              <select id="subject" name="subject" required>
                <option value="">Choisis un sujet</option>
                <option value="formation">Formation</option>
                <option value="coaching">Coaching privé</option>
                <option value="partnership">Partenariat</option>
                <option value="other">Autre</option>
              </select>
            </div>
            
            <div class="form-group">
              <label for="message">Message</label>
              <textarea id="message" name="message" rows="6" required></textarea>
            </div>
            
            <button type="submit" class="submit-btn">
              Envoyer le message
            </button>
            
            <!-- Message de confirmation -->
            <div id="form-message" class="form-message" style="display: none;">
              <p>✅ Message envoyé avec succès ! Je te réponds sous 24h.</p>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .contact-page {
    min-height: 100vh;
    padding-top: 100px;
  }

  .contact-hero {
    text-align: center;
    padding: 80px 20px 60px;
    max-width: 800px;
    margin: 0 auto;
  }

  .contact-hero h1 {
    font-size: clamp(48px, 8vw, 72px);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 24px;
    letter-spacing: -0.04em;
    background: linear-gradient(180deg, #FFFFFF 0%, #636366 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 20px;
    color: #8E8E93;
    line-height: 1.5;
    max-width: 600px;
    margin: 0 auto;
  }

  .contact-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px 80px;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 80px;
    margin-top: 60px;
  }

  .contact-info h2,
  .contact-form h2 {
    font-size: 28px;
    font-weight: 700;
    color: #FFFFFF;
    margin-bottom: 32px;
    letter-spacing: -0.02em;
  }

  .contact-item {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    margin-bottom: 32px;
    padding: 24px;
    background: linear-gradient(145deg, #1C1C1E, rgba(0, 122, 255, 0.05));
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
  }

  .contact-icon {
    font-size: 24px;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 122, 255, 0.1);
    border-radius: 12px;
    flex-shrink: 0;
  }

  .contact-item h3 {
    font-size: 18px;
    font-weight: 600;
    color: #FFFFFF;
    margin-bottom: 8px;
  }

  .contact-item p {
    color: #8E8E93;
    font-size: 16px;
    margin: 0;
  }

  .contact-form {
    background: linear-gradient(145deg, #1C1C1E, rgba(0, 122, 255, 0.05));
    border-radius: 24px;
    padding: 40px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
  }

  .form-group {
    margin-bottom: 24px;
  }

  .form-group label {
    display: block;
    color: #FFFFFF;
    font-weight: 500;
    margin-bottom: 8px;
    font-size: 14px;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 16px 20px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    color: #FFFFFF;
    font-size: 16px;
    font-family: inherit;
    transition: all 0.3s ease;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: #007AFF;
    background: rgba(255, 255, 255, 0.08);
    box-shadow: 0 0 0 4px rgba(0, 122, 255, 0.1);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: #636366;
  }

  .submit-btn {
    width: 100%;
    padding: 18px 32px;
    background: linear-gradient(135deg, #007AFF 0%, #0051D5 100%);
    color: #FFFFFF;
    border: none;
    border-radius: 12px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 16px;
  }

  .submit-btn:hover {
    background: linear-gradient(135deg, #0051D5 0%, #0039A6 100%);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 122, 255, 0.3);
  }

  .submit-btn:disabled {
    background: #48484A;
    cursor: not-allowed;
    transform: none;
    opacity: 0.6;
  }

  .form-message {
    margin-top: 20px;
    padding: 16px 20px;
    background: rgba(52, 199, 89, 0.1);
    border: 1px solid rgba(52, 199, 89, 0.2);
    border-radius: 12px;
    text-align: center;
    opacity: 0;
    transform: translateY(-10px);
    transition: all 0.4s ease;
  }

  .form-message.show {
    opacity: 1;
    transform: translateY(0);
  }

  .form-message p {
    color: #34C759;
    font-weight: 500;
    margin: 0;
    font-size: 16px;
  }

  .form-message.error {
    background: rgba(255, 59, 48, 0.1);
    border: 1px solid rgba(255, 59, 48, 0.2);
  }

  /* Mobile Optimization */
  @media (max-width: 768px) {
    .contact-hero {
      padding: 60px 20px 40px;
    }

    .contact-hero h1 {
      font-size: 42px;
    }

    .hero-subtitle {
      font-size: 18px;
    }

    .contact-content {
      padding: 0 20px 60px;
    }

    .contact-grid {
      grid-template-columns: 1fr;
      gap: 40px;
      margin-top: 40px;
    }

    .contact-form {
      padding: 30px 24px;
    }

    .contact-info h2,
    .contact-form h2 {
      font-size: 24px;
      margin-bottom: 24px;
    }

    .contact-item {
      padding: 20px;
      margin-bottom: 20px;
    }

    .contact-icon {
      width: 40px;
      height: 40px;
      font-size: 20px;
    }
  }

  @media (max-width: 480px) {
    .contact-hero h1 {
      font-size: 36px;
    }

    .hero-subtitle {
      font-size: 16px;
    }

    .contact-form {
      padding: 24px 20px;
    }

    .form-group input,
    .form-group select,
    .form-group textarea {
      padding: 14px 16px;
      font-size: 14px;
    }

    .submit-btn {
      padding: 16px 24px;
      font-size: 14px;
    }
  }
</style>

<script>
  // Amélioration de l'expérience utilisateur du formulaire
  document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('form[name="contact"]');
    const submitBtn = form.querySelector('.submit-btn');
    const formMessage = document.getElementById('form-message');
    
    // Gestion de la soumission du formulaire
    form.addEventListener('submit', function(e) {
      e.preventDefault();
      
      // Désactive le bouton pendant l'envoi
      submitBtn.disabled = true;
      submitBtn.textContent = 'Envoi en cours...';
      
      // Créer FormData pour l'envoi
      const formData = new FormData(form);
      
      // Envoyer via fetch à Netlify
      fetch('/', {
        method: 'POST',
        headers: { "Content-Type": "application/x-www-form-urlencoded" },
        body: new URLSearchParams(formData).toString()
      })
      .then(response => {
        if (response.ok) {
          // Succès - afficher le message
          formMessage.innerHTML = '<p>✅ Message envoyé avec succès ! Je te réponds sous 24h.</p>';
          formMessage.className = 'form-message show';
          
          // Réinitialiser le formulaire
          form.reset();
          
          // Faire défiler vers le message
          setTimeout(() => {
            formMessage.scrollIntoView({ behavior: 'smooth', block: 'center' });
          }, 100);
          
          // Réactiver le bouton après 3 secondes
          setTimeout(() => {
            submitBtn.disabled = false;
            submitBtn.textContent = 'Envoyer le message';
          }, 3000);
        } else {
          throw new Error('Erreur réseau');
        }
      })
      .catch(error => {
        // Erreur - afficher le message d'erreur
        formMessage.innerHTML = '<p style="color: #FF3B30;">❌ Erreur lors de l\'envoi. Réessaie ou contacte-moi directement à info@sonnycourt.com</p>';
        formMessage.className = 'form-message show error';
        
        // Réactiver le bouton
        submitBtn.disabled = false;
        submitBtn.textContent = 'Envoyer le message';
      });
    });
    
    // Validation en temps réel
    const inputs = form.querySelectorAll('input[required], select[required], textarea[required]');
    inputs.forEach(input => {
      input.addEventListener('blur', function() {
        if (this.value.trim() === '') {
          this.style.borderColor = '#FF3B30';
        } else {
          this.style.borderColor = '#34C759';
        }
      });
      
      input.addEventListener('input', function() {
        if (this.value.trim() !== '') {
          this.style.borderColor = 'rgba(255, 255, 255, 0.1)';
        }
      });
    });
    
    // Validation email
    const emailInput = document.getElementById('email');
    emailInput.addEventListener('blur', function() {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailRegex.test(this.value)) {
        this.style.borderColor = '#FF3B30';
      } else {
        this.style.borderColor = '#34C759';
      }
    });
  });
</script>
