---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Page non trouvée - Sonny Court">
  <div class="not-found">
    <div class="nf-inner">
      <div class="nf-logo">SC</div>
      <h1 class="nf-code">404</h1>
      <h2 class="nf-title">Oups ! Cette page n'existe pas</h2>
      <p class="nf-text">La page que tu recherches semble avoir disparu ou n'a jamais existé. Pas de panique, on va te remettre sur le bon chemin !</p>
      <div class="nf-actions">
        <a href="/" class="btn-primary">Retour à l'accueil</a>
        <div class="nf-links">
          <a href="/formations" class="btn-secondary">Nos formations</a>
          <a href="/contact" class="btn-secondary">Nous contacter</a>
        </div>
      </div>
      <div class="nf-quote">
        <p>"Chaque erreur est une opportunité d'apprendre quelque chose de nouveau." <span class="nf-quote-author">- Sonny Court</span></p>
      </div>
    </div>
  </div>
</Layout>

<style>
  .not-found { min-height: calc(100vh - 0px); display: flex; align-items: center; justify-content: center; padding: 40px 16px; }
  .nf-inner { width: 100%; max-width: 720px; text-align: center; animation: fadeIn .6s ease-out; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(20px);} to { opacity:1; transform: translateY(0);} }
  .nf-logo { width: 96px; height: 96px; margin: 0 auto 24px; border-radius: 9999px; display: flex; align-items: center; justify-content: center; color: #fff; font-weight: 800; font-size: 28px; background: linear-gradient(90deg, #2563eb, #4f46e5); box-shadow: 0 10px 20px rgba(37, 99, 235, 0.35); }
  .nf-code { color: #fff; font-weight: 800; font-size: 64px; line-height: 1.1; margin: 0 0 8px; }
  .nf-title { color: #d1d5db; font-weight: 700; font-size: 22px; margin: 0 0 20px; }
  .nf-text { color: #9ca3af; font-size: 18px; line-height: 1.6; margin: 0 auto 28px; max-width: 56ch; }
  .nf-actions { margin-bottom: 32px; }
  .btn-primary { display: inline-block; padding: 12px 24px; border-radius: 10px; background: linear-gradient(90deg, #2563eb, #4f46e5); color: #fff; font-weight: 700; text-decoration: none; box-shadow: 0 10px 20px rgba(37, 99, 235, 0.35); transition: transform .2s ease, filter .2s ease; }
  .btn-primary:hover { transform: translateY(-2px); filter: brightness(1.05); }
  .nf-links { display: flex; gap: 12px; justify-content: center; margin-top: 16px; flex-wrap: wrap; }
  .btn-secondary { display: inline-block; padding: 10px 18px; border-radius: 10px; border: 1px solid rgba(59,130,246,.35); color: #60a5fa; background: rgba(255,255,255,0.02); text-decoration: none; transition: background .2s ease, transform .2s ease; }
  .btn-secondary:hover { background: rgba(59,130,246,.08); transform: translateY(-2px); }
  .nf-quote { background: rgba(31,41,55,0.6); border: 1px solid rgba(75,85,99,0.3); border-radius: 12px; padding: 18px; }
  .nf-quote p { color: #d1d5db; font-style: italic; margin: 0; }
  .nf-quote-author { display: block; margin-top: 8px; color: #9ca3af; font-size: 14px; }
</style>
