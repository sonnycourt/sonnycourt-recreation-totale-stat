<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offre Flash Exclusive - Manifest</title>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
            background: #0a0014;
            color: #ffffff;
            line-height: 1.6;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* Fond avec effet cosmique */
        .cosmic-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(ellipse at center, #1a0033 0%, #0a0014 100%);
            z-index: -2;
        }

        .cosmic-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background:
                radial-gradient(circle at 20% 50%, rgba(138, 43, 226, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(255, 0, 255, 0.05) 0%, transparent 50%);
            z-index: -1;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        /* Animation glow */
        @keyframes glow {
            0% { box-shadow: 0 0 20px rgba(138, 43, 226, 0.5); }
            50% { box-shadow: 0 0 40px rgba(138, 43, 226, 0.8), 0 0 60px rgba(255, 0, 255, 0.4); }
            100% { box-shadow: 0 0 20px rgba(138, 43, 226, 0.5); }
        }

        /* En-tête d'urgence */
        .urgency-header {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0.01));
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            text-align: center;
            padding: 25px 30px;
            margin-bottom: 40px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }

        .urgency-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 200%;
            height: 2px;
            background: linear-gradient(90deg, transparent, #ff1744, transparent);
            animation: scanline 3s linear infinite;
        }

        @keyframes scanline {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        .urgency-header::after {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 50% 50%, rgba(255, 23, 68, 0.1) 0%, transparent 70%);
            pointer-events: none;
        }

        .warning-icon {
            display: inline-block;
            width: 24px;
            height: 24px;
            margin-right: 10px;
            vertical-align: middle;
            animation: pulse-icon 2s ease-in-out infinite;
        }

        @keyframes pulse-icon {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }

        .processing {
            color: rgba(255, 255, 255, 0.7);
            font-size: 16px;
            font-weight: 400;
            margin-bottom: 8px;
            letter-spacing: 0.3px;
        }

        .urgency-text {
            font-size: 17px;
            font-weight: 500;
            color: rgba(255, 255, 255, 0.8);
            letter-spacing: 0.2px;
        }

        /* Hero Section */
        .hero-section {
            text-align: center;
            margin-bottom: 50px;
        }

        .badge-exclusive {
            display: inline-block;
            background: rgba(138, 43, 226, 0.2);
            border: 1px solid rgba(138, 43, 226, 0.3);
            padding: 6px 20px;
            border-radius: 100px;
            font-size: 13px;
            font-weight: 500;
            letter-spacing: 1.2px;
            margin-bottom: 30px;
            text-transform: uppercase;
            color: rgba(255, 255, 255, 0.8);
        }

        .main-title {
            font-size: 56px;
            font-weight: 600;
            background: linear-gradient(135deg, #ffffff 0%, #8a2be2 50%, #ff00ff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
            line-height: 1.1;
            letter-spacing: -2px;
        }

        @media (min-width: 768px) {
            .main-title {
                font-size: 72px !important;
            }
        }

        @media (min-width: 1024px) {
            .main-title {
                font-size: 96px !important;
            }
        }

        @media (min-width: 1200px) {
            .main-title {
                font-size: 120px !important;
            }
        }

        .subtitle {
            font-size: 22px;
            color: rgba(255, 255, 255, 0.7);
            font-weight: 400;
            margin-bottom: 40px;
            line-height: 1.4;
        }

        /* Image hero */
        .hero-image {
            width: 100%;
            max-width: 600px;
            height: auto;
            border-radius: 30px;
            margin: 0 auto 40px;
            display: block;
            box-shadow: 0 30px 60px rgba(138, 43, 226, 0.3);
        }

        /* Prix Section */
        .price-section {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 30px;
            padding: 40px;
            margin-bottom: 30px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
        }

        .price-badge {
            background: #ff1744;
            color: white;
            padding: 6px 16px;
            border-radius: 100px;
            font-size: 14px;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .price-container {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin-bottom: 10px;
        }

        .old-price {
            font-size: 32px;
            color: rgba(255, 255, 255, 0.3);
            text-decoration: line-through;
            font-weight: 400;
        }

        .new-price {
            font-size: 56px;
            font-weight: 800;
            color: #ffffff;
        }

        .price-note {
            color: rgba(255, 255, 255, 0.6);
            font-size: 16px;
        }

        /* CTA Button */
        .cta-button {
            display: block;
            width: 100%;
            max-width: 600px;
            margin: 30px auto;
            padding: 24px 40px;
            font-size: 20px;
            font-weight: 700;
            text-align: center;
            text-decoration: none;
            border-radius: 100px;
            position: relative;
            overflow: hidden;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .cta-primary {
            background: linear-gradient(135deg, #ff9500, #ff5e00);
            color: white;
            box-shadow: 0 10px 40px rgba(255, 149, 0, 0.4);
        }

        .cta-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 50px rgba(255, 149, 0, 0.6);
        }

        .cta-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .cta-primary:hover::before {
            left: 100%;
        }

        /* Modules Grid */
        .modules-section {
            margin: 60px 0;
        }

        .section-title {
            font-size: 48px;
            font-weight: 700;
            text-align: center;
            margin-bottom: 50px;
            color: #ffffff;
            font-family: 'Bebas Neue', sans-serif;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        @media (max-width: 768px) {
            .section-title {
                font-size: 32px;
                letter-spacing: 2px;
                margin-bottom: 30px;
                padding: 0 15px;
                word-wrap: break-word;
                hyphens: auto;
            }
        }

        .modules-grid {
            display: grid;
            gap: 20px;
        }

        .module-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 30px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .module-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, #ff00ff, #8a2be2);
            transform: translateX(-100%);
            transition: transform 0.3s ease;
        }

        .module-card:hover::before {
            transform: translateX(0);
        }

        .module-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(138, 43, 226, 0.3);
        }

        .module-number {
            display: inline-block;
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #8a2be2, #6a1b9a);
            border-radius: 50%;
            text-align: center;
            line-height: 40px;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .module-title {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 12px;
            color: #ffffff;
        }

        .module-description {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .module-benefit {
            color: #ffffff;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
            opacity: 0.9;
        }

        /* Bonus Section */
        .bonus-section {
            background: linear-gradient(135deg, rgba(138, 43, 226, 0.1), rgba(255, 0, 255, 0.1));
            border-radius: 30px;
            padding: 50px 40px;
            margin: 60px 0;
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }

        .bonus-section::before {
            content: '🎁';
            position: absolute;
            top: -50px;
            right: -50px;
            font-size: 200px;
            opacity: 0.05;
            transform: rotate(-15deg);
        }

        .bonus-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }

        @media (max-width: 768px) {
            .bonus-grid {
                grid-template-columns: 1fr;
                gap: 20px;
                margin-top: 20px;
                justify-items: center;
            }

            .bonus-item {
                width: 100%;
                max-width: 350px;
                text-align: center;
            }
        }

        .bonus-item {
            background: rgba(0, 0, 0, 0.3);
            padding: 25px;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }

        .bonus-item:hover {
            transform: translateY(-3px);
            border-color: rgba(138, 43, 226, 0.5);
        }

        .bonus-title {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 10px;
            color: #ffffff;
        }

        .bonus-value {
            color: #ff9500;
            font-weight: 700;
            font-size: 20px;
            margin-bottom: 10px;
        }

        /* Garantie */
        .guarantee {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0.01));
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 40px;
            text-align: center;
            margin: 40px 0;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
        }

        .guarantee::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, #8a2be2, transparent, #ff9500);
            border-radius: 24px;
            opacity: 0.3;
            z-index: -1;
            animation: shimmer 3s ease-in-out infinite;
        }

        @keyframes shimmer {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 0.5; transform: scale(1.01); }
        }

        .guarantee-content {
            position: relative;
            z-index: 1;
        }

        .guarantee-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #8a2be2, #ff9500);
            border-radius: 50%;
            margin: 0 auto 20px;
            font-size: 40px;
            box-shadow: 0 10px 30px rgba(138, 43, 226, 0.3);
        }

        .guarantee h3 {
            font-size: 28px;
            margin-bottom: 15px;
            color: #ffffff;
            font-weight: 700;
            letter-spacing: -0.5px;
        }

        .guarantee-subtitle {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 20px;
        }

        .guarantee-features {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .guarantee-feature {
            display: flex;
            align-items: center;
            gap: 8px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 16px;
        }

        .guarantee-feature svg {
            width: 20px;
            height: 20px;
            color: #8a2be2;
        }

        @media (max-width: 768px) {
            .guarantee-features {
                flex-direction: column;
                gap: 15px;
                align-items: flex-start;
            }

            .guarantee-feature {
                justify-content: flex-start;
                text-align: left;
                min-width: 200px;
                width: 100%;
            }
        }

        /* Timer */
        .countdown-timer {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0.01));
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 20px 30px;
            text-align: center;
            font-size: 16px;
            font-weight: 500;
            color: rgba(255, 255, 255, 0.8);
            display: inline-block;
            position: relative;
            overflow: hidden;
        }



        .timer-content {
            position: relative;
            z-index: 1;
            display: flex;
            align-items: center;
            gap: 15px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .timer-label {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.6);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .timer-display {
            display: flex;
            gap: 8px;
            align-items: center;
        }

        .timer-unit {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            padding: 8px 12px;
            min-width: 40px;
            text-align: center;
        }

        .timer-number {
            font-size: 24px;
            font-weight: 700;
            color: #ff9500;
            font-family: 'Bebas Neue', monospace;
            letter-spacing: 1px;
        }

        @media (max-width: 768px) {
            .countdown-timer {
                padding: 15px 20px;
                margin: 15px auto 0;
                width: 90%;
                max-width: 350px;
            }

            .timer-content {
                gap: 10px;
                flex-direction: column;
            }

            .timer-label {
                font-size: 12px;
            }

            .timer-number {
                font-size: 20px;
            }

            .timer-unit {
                padding: 6px 10px;
                min-width: 35px;
            }
        }

        .timer-separator {
            font-size: 20px;
            color: #ff9500;
            font-weight: 700;
            animation: blink 1s ease-in-out infinite;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.3; }
        }

        .timer-icon {
            width: 20px;
            height: 20px;
            color: #ff1744;
            animation: rotate 2s linear infinite;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* CTA Secondaire Button */
        .cta-secondary-button {
            background: rgba(255, 255, 255, 0.05);
            color: rgba(255, 255, 255, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 16px;
            padding: 18px 30px;
            font-weight: 400;
            letter-spacing: 0;
            text-transform: none;
            margin-top: -10px;
            transition: all 0.3s ease;
        }

        .cta-secondary-button:hover {
            background: rgba(255, 255, 255, 0.08);
            color: rgba(255, 255, 255, 0.6);
            box-shadow: none;
            transform: none;
        }

        /* Refus */
        .cta-secondary {
            background: transparent;
            color: rgba(255, 255, 255, 0.5);
            border: none;
            font-size: 16px;
            padding: 15px;
            text-decoration: underline;
            font-weight: 400;
            margin-top: 20px;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .cta-secondary:hover {
            color: rgba(255, 255, 255, 0.8);
        }

        /* Floating CTA Button */
        .floating-cta {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
            background: linear-gradient(135deg, #ff9500, #ff5e00);
            color: white;
            padding: 15px 25px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            font-size: 16px;
            box-shadow: 0 8px 25px rgba(255, 149, 0, 0.4);
            transition: all 0.3s ease;
            animation: float 3s ease-in-out infinite;
        }

        .floating-cta:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(255, 149, 0, 0.6);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-5px); }
        }

        @media (max-width: 768px) {
            .floating-cta {
                bottom: 15px;
                right: 15px;
                padding: 12px 20px;
                font-size: 14px;
            }
        }

        /* Mobile responsive */
        @media (max-width: 768px) {
            .container {
                padding: 20px 15px;
            }

            .main-title {
                font-size: 56px !important;
            }

            .subtitle {
                font-size: 18px;
            }

            .price-container {
                flex-direction: column;
                gap: 10px;
            }

            .new-price {
                font-size: 42px;
            }

            .cta-button {
                font-size: 18px;
                padding: 20px 30px;
            }
        }
    </style>
</head>
<body>
    <div class="cosmic-bg"></div>
    <div class="cosmic-overlay"></div>

    <div class="container">
        <!-- En-tête d'urgence -->
        <div class="urgency-header">
            <div style="color: #ff1744; font-size: 18px; font-weight: 700; margin-bottom: 12px; display: flex; align-items: center; justify-content: center;">
                <svg class="warning-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                </svg>
                ATTENTION : OFFRE FLASH - NE FERMEZ PAS CETTE PAGE !
            </div>
            <div class="processing">Ta commande est en cours de traitement.</div>
            <div class="urgency-text">Cette offre exclusive ne sera plus disponible après.</div>
        </div>

        <!-- Hero Section -->
        <div class="hero-section">
            <span class="badge-exclusive">✨ Offre VIP Exclusive</span>

            <h1 class="main-title">MANIFEST</h1>
            <p class="subtitle">3 mois pour manifester<br>la richesse, l'amour et la liberté</p>

            <!-- Image principale -->
            <img src="https://sonnycourt.com/wp-content/uploads/2025/07/Sonny-Court-Manifest-Formation.webp" alt="Manifest - Sonny Court" class="hero-image">
        </div>

        <!-- Prix Section -->
        <div class="price-section">
            <span class="price-badge">-60% AUJOURD'HUI SEULEMENT</span>
            <div class="price-container">
                <span class="old-price">497€</span>
                <span class="new-price">198.80€</span>
            </div>
            <p class="price-note">(Économisez 298€ avec cette offre limitée)</p>

            <!-- Timer dans la section prix -->
            <div class="countdown-timer" style="margin: 20px auto 0;">
                <div class="timer-content">
                    <svg class="timer-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span class="timer-label">Offre expire dans</span>
                    <div class="timer-display">
                        <div class="timer-unit">
                            <div class="timer-number" id="minutes">05</div>
                        </div>
                        <div class="timer-separator">:</div>
                        <div class="timer-unit">
                            <div class="timer-number" id="seconds">00</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- CTA Principal -->
        <a href="https://sonnycourt.spiffy.co/offer/zB7dF7ul" class="cta-button cta-primary">
            🚀 OUI ! J'ajoute MANIFEST maintenant
        </a>

        <!-- CTA Secondaire -->
        <a href="https://sonnycourt.spiffy.co/offer/BZ6eCxur" class="cta-button cta-secondary-button">
            Non merci, je préfère payer le prix fort plus tard
        </a>

        <!-- Modules -->
        <div class="modules-section">
            <h2 class="section-title">Votre transformation en 3 modules</h2>

            <div class="modules-grid">
                <div class="module-card">
                    <div class="module-number">1</div>
                    <h3 class="module-title">Fondations de l'Esprit</h3>
                    <p class="module-description">Libérez-vous de vos croyances limitantes et reprenez le contrôle total de vos pensées.</p>
                    <div class="module-benefit">
                        <span>→</span> Transformez votre mental en 30 jours
                    </div>
                </div>

                <div class="module-card">
                    <div class="module-number">2</div>
                    <h3 class="module-title">Expansion Intérieure</h3>
                    <p class="module-description">Méditations avancées pour aligner vos désirs profonds avec votre réalité quotidienne.</p>
                    <div class="module-benefit">
                        <span>→</span> Multipliez les synchronicités
                    </div>
                </div>

                <div class="module-card">
                    <div class="module-number">3</div>
                    <h3 class="module-title">Maîtrise et Manifestation</h3>
                    <p class="module-description">Techniques de visualisation puissantes pour matérialiser vos objectifs rapidement.</p>
                    <div class="module-benefit">
                        <span>→</span> Manifestez 10x plus vite
                    </div>
                </div>
            </div>
        </div>

        <!-- Bonus Section -->
        <div class="bonus-section">
            <h2 class="section-title">Bonus exclusifs inclus</h2>

            <div class="bonus-grid">
                <div class="bonus-item">
                    <div class="bonus-title">📚 3 Livres Audio Premium</div>
                    <div class="bonus-value">Valeur : 90€</div>
                    <p>Les secrets inédits de Franklin, Barnum et Brown</p>
                </div>

                <div class="bonus-item">
                    <div class="bonus-title">📖 Bibliothèque Essentielle</div>
                    <div class="bonus-value">Valeur : 30€</div>
                    <p>12 livres pour maîtriser la loi de l'attraction</p>
                </div>

                <div class="bonus-item">
                    <div class="bonus-title">👥 Communauté Privée VIP</div>
                    <div class="bonus-value">Valeur : 180€</div>
                    <p>6 mois d'accès au cercle exclusif</p>
                </div>
            </div>
        </div>

        <!-- CTA Final -->
        <a href="https://sonnycourt.spiffy.co/offer/zB7dF7ul" class="cta-button cta-primary">
            💫 Accéder à MANIFEST maintenant (-60%)
        </a>

        <!-- CTA Secondaire -->
        <a href="https://sonnycourt.spiffy.co/offer/BZ6eCxur" class="cta-button cta-secondary-button">
            Non merci, je préfère payer le prix fort plus tard
        </a>

        <!-- Testimonials Section -->
        <section style="margin: 60px 0;">
            <h2 style="text-align: center; font-size: 3rem; margin-bottom: 40px; color: #ffffff; font-family: 'Bebas Neue', sans-serif;">
                Ce que disent nos membres
            </h2>

            <!-- Elfsight Testimonials Slider | MANIFEST -->
            <script src="https://static.elfsight.com/platform/platform.js" async></script>
            <div class="elfsight-app-2af569cb-eeca-4673-b866-0f34bc463e6e" data-elfsight-app-lazy></div>
        </section>

        <!-- Garantie -->
        <div class="guarantee">
            <div class="guarantee-content">
                <div class="guarantee-badge">✓</div>
                <h3>Garantie Totale 30 Jours</h3>
                <p class="guarantee-subtitle">Testez MANIFEST sans aucun risque</p>

                <div class="guarantee-features">
                    <div class="guarantee-feature">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <span>Sans questions</span>
                    </div>
                    <div class="guarantee-feature">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <span>Remboursement rapide</span>
                    </div>
                    <div class="guarantee-feature">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <span>100% sécurisé</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Configuration Spiffy pour ouvrir dans le même onglet
        document.addEventListener('DOMContentLoaded', function() {
            // Intercepter tous les liens Spiffy et forcer l'ouverture dans le même onglet
            const spiffyLinks = document.querySelectorAll('a[href*="spiffy.co"]');
            spiffyLinks.forEach(function(link: Element) {
                link.addEventListener('click', function(this: HTMLElement) {
                    // Laisser Spiffy gérer le clic mais forcer le même onglet
                    this.setAttribute('target', '_self');
                });
            });
        });

        // Timer countdown
        let totalSeconds = 5 * 60; // 5 minutes
        const timerElement = document.querySelector('.countdown-timer');

        setInterval(() => {
            if (totalSeconds > 0) {
                totalSeconds--;
                const minutes = Math.floor(totalSeconds / 60);
                const seconds = totalSeconds % 60;

                const minutesElement = document.getElementById('minutes');
                const secondsElement = document.getElementById('seconds');

                if (minutesElement) {
                    minutesElement.textContent = minutes.toString().padStart(2, '0');
                }
                if (secondsElement) {
                    secondsElement.textContent = seconds.toString().padStart(2, '0');
                }
            } else if (totalSeconds === 0) {
                // Redirection automatique en cliquant sur le bouton secondaire
                const nonButton = document.querySelector('.cta-secondary-button') as HTMLElement;
                if (nonButton) {
                    nonButton.click();
                }
            }
        }, 1000);
    </script>

    <!-- Floating CTA Button -->
    <a href="https://sonnycourt.spiffy.co/offer/zB7dF7ul" class="floating-cta">
        ⚡ OUI, J'AJOUTE !
    </a>
</body>
</html>
